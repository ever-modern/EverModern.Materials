# Copilot Instructions

## Project Guidelines
- Exclude autogenerated files when adding XML documentation in Utilities projects.
- For join APIs in query facades, accept only the project's queryable abstraction interface types; do not accept raw IQueryable.